# ChiefUI Assistant - backend bagimliliklari
# ---------------------------------------------------------------------------
# torch'u kendi CUDA surumune gore kur (GTX 1660 Ti / Turing icin cu126 dogrulandi):
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
# Ayni wheel hem GPU (--device cuda) hem CPU (--device cpu) calistirir.
# Once torch'u kur, SONRA: pip install -r requirements.txt
# ---------------------------------------------------------------------------
torch>=2.1.0
numpy>=1.24
tokenizers>=0.15          # KENDI BPE'mizi egitmek icin (hazir model degil)
tqdm>=4.66

fastapi>=0.110
uvicorn[standard]>=0.27   # websockets dahil
pydantic>=2.0

# Opsiyonel (8-bit optimizer, VRAM tasarrufu):
# bitsandbytes>=0.43 ; platform_system != "Darwin"
